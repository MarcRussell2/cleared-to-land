CLEARED TO LAND art pass 3b of the world rebuild: VEGETATION, second round - the review of pass 3 with the screenshots and the probe, and what to fix.

You are the art department. Read AGENTS.md at the root, then src/art/AGENTS.md (section "The world bench"), then src/art/README.md. Same fence as pass 3, TWO files: src/art/world-vegetation.js and the `trees` block of src/art/palette.js. Pass 3 is committed; edit in place.

WHAT PASS 3 GOT RIGHT (keep it): the hedgerows - upright hedges along the parcel edges with gate gaps and hedge trees, the "green slugs" are gone; the four tree shapes with a lit and a shaded side; the ragged woodland margins on the plains; the horizon treeline; triangles at the plains site fell from 4.66 M to 0.60 M and at the mountain sites from 5.4 M to 0.63 M.

WHAT THE SCREENSHOTS AND THE PROBE SHOW IS WRONG:

1. THE MOUNTAIN FORESTS ARE GONE. The valleys had 45,700 trees; pass 3 places 9,282 spruce and 882 poplar, and the billboard layer is too sparse and too small to read as a forest, so the valley walls of the gravel bar and the ridge strip - which were carpeted in spruce up to the treeline - are now bare green slopes with a few trees near the strip. Restore the carpet: on high, the mountain sites get their trees back (35-45k, the same spacing as before: about 14 m on the woodland slopes, thinning to the treeline), the plains about 20k. Triangles allow it: a billboard is 4 triangles, so 40k far trees are 160k. The far forest must read as a dense, dark, slightly textured carpet from 2-6 km, exactly as the old cones did, only better shaped.

2. THE LEVEL OF DETAIL IS CHOSEN PER CHUNK AND THE CHUNKS ARE TOO BIG. With 1.8 km chunks switching at 1.25/2.4 km from the chunk centre, a tree 200 m from the tower camera renders as a billboard (the tower scene's foreground is flat dark cut-outs with pale fringes), while 28 billboard chunks draw at the mountain frame (28 draw calls for what should be one). Change the design to chunk-level culling plus PER-INSTANCE ranges:
   - Keep chunks (2-2.4 km is fine) with one InstancedMesh per species per level, bounding spheres computed, frustum culled by the renderer. Do not use THREE.LOD for the switch.
   - In each level's material (MeshStandardMaterial with onBeforeCompile, uniforms prefixed `vg`, a versioned customProgramCacheKey), the vertex shader computes the instance's world position from instanceMatrix, its distance to cameraPosition, and collapses the vertex to the instance origin (scale to zero) when that distance is outside the level's range: level 0 within 350 m (high; 220 m medium; 120 m low), level 1 from there to 1500 m (900 m medium; 500 m low), billboards beyond. Add a 30 m cross-fade band if you can (alternate instances switch a little earlier - a per-instance hash offset on the threshold), so nothing pops in a row.
   - The billboards are ONE InstancedMesh per species for the whole site (not per chunk): 4 triangles each, camera-facing in the vertex shader, collapsed within the level-1 range. That is 2-4 draws for every far tree on the site. Draw calls for all vegetation in a typical frame: under 30 on high.
   - castShadow only on the level-0 meshes; receiveShadow off everywhere.

3. THE BILLBOARDS MUST LOOK LIKE TREES. Their texture (a canvas per species, 128x128 or 256x256, cached module-wide by seed and species): the tree's silhouette with a lit half and a shaded half so the far forest has direction, the crown colour matching the level-0 material, the edge pixels dilated with the crown colour so there is no pale fringe, alphaTest 0.5, no additive blending; sized to the instance's real height and width; fog-enabled so the atmosphere hazes them like everything else. Per-instance colour (setColorAt) as on the other levels.

4. THE SPRUCE ARE TOO PALE. They read as mint green. Real spruce is dark: crown albedo around 0.06-0.10 red, 0.11-0.16 green, 0.06-0.09 blue linear, slightly bluer in shade; pine a touch warmer; broadleaf mid-green 0.10-0.18 green with a yellowish lit side; the treeline larch-like conifers paler and yellower. Put the numbers in PALETTE.trees (keep the existing keys) and apply them to all three levels alike so a tree does not change colour when it switches level.

5. Keep: OBSTACLE_TREE matching the obstacle spruce's real size (you set 4.04 m x 25 m - fine, keep them in step), the exported names, the file header (update it), material names, no Math.random, no module-level material or geometry caches (textures only), frustumCulled left on with computed bounds, hedges as they are.

CHECK YOUR WORK: `node tools/test-art.mjs` must pass. In your answer: the tree counts per site and tier, the expected draw calls and triangles for the plains and the mountain sites at a typical frame, and what these scenes should now look like: the mountain valley 09:30 (a spruce-carpeted valley wall on both sides, riverside broadleaves, the treeline); the ridge strip 17:00 wing view (forest on the slopes); the tower 16:30 (real trees in the foreground, a forest line behind); plains 10:00 chase (unchanged apart from colour); the gravel bar's spruce wall.
