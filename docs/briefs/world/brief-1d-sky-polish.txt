CLEARED TO LAND art pass 1d of the world rebuild: THE SKY, polish round - performance findings and the last two looks.

You are the art department. Read AGENTS.md at the root, then src/art/AGENTS.md (section "The world bench"), then src/art/README.md, then docs/PERF.md section 5 ("sky.js") and section 9.3. Same fence as the sky passes, THREE files: src/art/world-atmosphere.js, src/art/sky.js, and the `atmosphere` block of src/art/palette.js. Nothing else. Your passes 1-1c are committed and accepted; edit in place. The owner's eleven-scene sheet and the cost probe run after you hand back.

WHAT THE SKY IS NOW (keep all of it): the scattering lookup baked at set(), atmosphere() as two fetches, the radiance budget (sunlit white about 0.8, the sky under 0.85, exposure 1.24 by day and 3.8 at night), clearExtinction(V) = 3.912/(3.0 V) in clear air blending to Koschmieder in weather (the owner raised the divisor from 2.2 to 3.0 after your pass; tools/sky-budget.mjs keeps the limits), the darker airlight on ground-bound rays, the C1 mist through the horizon, the cumulus from seeded clusters with a period-breaking second sample, the stars faded by sky brightness, the moon light hidden by day (the performance branch added `this.moon.visible = intensity > 0.002`).

MEASURED REQUIREMENTS from the performance branch (docs/PERF.md 9.3, the Pixel proxy at phone medium): the sky dome costs 0.4 ms and the clouds 0.3 ms - fine, keep them there. Two budget breaches are yours:
1. "a light visible at intensity 0": at night the SUN light is visible at intensity 0, and every lit shader pays for a directional light it cannot see. Hide the sun by night exactly as the moon is hidden by day (`this.sun.visible = this.sun.intensity > 0.002`), including its shadow (a hidden light casts none). Mind the transition through dusk: the moon comes on as the sun goes off; never both invisible while there is any light, and never a frame where shadows pop because the caster changed - the dayness blend is 12 degrees wide, so the sun is visible until it is truly dark.
2. Every transparent DoubleSide material you own must carry `forceSinglePass: true` (the clouds already do - confirm) so three.js does not re-key its program per frame.

THE TWO LOOKS THAT ARE STILL OPEN - your decision on both, within the budget:
3. THE CUMULUS ARE SMOOTH-LOBED. On the cockpit and coast scenes the low clouds read as soft white balloons: rounded lobes with an even gradient, no fibrous edge, no flat base line, no shadowed underside structure. Give them the texture of real fair-weather cumulus: a flat, slightly ragged base with a dark grey underside band, cauliflower tops with small-scale relief (a fine seeded noise modulating the density near the edge only - not per-pixel noise in the shader: bake it into the density map), bright sunlit rims, and gaps that show sky. The baked map may go to 512 px if that is what the relief needs; keep the period-breaking sample.
4. THE TOWER SCENE (16:30, 25 km, a 3.5-degree lens from a 42 m tower onto ground 4-8 km away) still reads hazier than every other scene: fields legible but milky, the airplane grey against a grey-blue sky. The owner's tests: with atExtinction at 0 it is crisp; at 0.6x still milky; the sky's own radiance is within budget; the bloom, clouds and water are innocent. You decide what a real 25 km afternoon through a long lens should look like and make it so - options include a lower in-scatter weight for rays under the horizon (the current lerp(1, 0.72, ...) could go to 0.55), a slightly warmer and darker horizon band in the afternoon, or a visibility-dependent contrast curve - as long as tools/sky-budget.mjs still exits 0 and the plains, coast and mountain scenes keep their (good) haze.

RULES (each learned from a pass that went wrong): keep the file headers and update them; uniforms starting with `at` only in these files and all must exist in the constructor; no module-level material caches; name every material; a versioned customProgramCacheKey on any onBeforeCompile material (bump the string when you change injected code); no Math.random; keep the SkySystem API, the exported names and every existing uniform (world-water.js reads atSun, atDay, atLow, atWeather, atWarm, atMoon; installFog reads atDay, atWeather, atExtinction).

CHECK YOUR WORK: `node tools/test-art.mjs` and `node tools/sky-budget.mjs` must both pass (paste the sky-budget output). In your answer: what you changed, and what these scenes should now look like: the tower 16:30; cockpit 10:00 (the clouds); coast 13:00; carrier 22:30 (nothing should change at night except that the sun is gone from the light list).
