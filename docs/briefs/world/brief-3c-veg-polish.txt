CLEARED TO LAND art pass 3c of the world rebuild: VEGETATION, polish round - over the code that now implements your pass-3b contract.

You are the art department. Read AGENTS.md at the root, then src/art/AGENTS.md (section "The world bench", rules 10-12 are new), then src/art/README.md, then docs/PERF.md sections 5 ("terrain-look.js") and 9.3, then docs/briefs/world/brief-3b-veg-fix.txt: that brief was written for you, its contract was implemented by the owner's session when your usage limit ran out, and this round hands the result back to you as the art department. Same fence, TWO files: src/art/world-vegetation.js and the `trees` block of src/art/palette.js. Edit in place.

WHAT THE CODE DOES NOW (read the file header - it is the specification, keep it): 2400 m chunks, two species per chunk; level of detail per INSTANCE in the vertex shader (rangeMaterial: level 0 within 350 m on high, level 1 to 1500 m, a per-instance 30 m stagger, collapsed to a point outside the range) with THREE.LOD only culling a 600 m (level 0) / 1200 m (level 1) cell wholly out of range; the far forest one camera-facing card mesh per species for the whole site (rangeMaterial with card = true: the quad is turned to the camera and its normal is forced to +Y so it is lit like flat ground); mountain valleys wooded wall to wall below the treeline (18 m cells, 65k default, 4800 per chunk), plains capped at 900 per chunk; hedges as 12 m segments with gates under a 2 km THREE.LOD; dark spruce (PALETTE.trees.conifer l = 0.065 - lightness is LINEAR in three's working space, rule 11). The performance branch then split the site-wide card meshes into spatial chunks itself (world/terrain.js, chunkInstanced) - you do not need to.

MEASURED (docs/PERF.md 9.3): the forest is 0.2 ms of GPU on the plains and 0.5 on the mountain at phone medium - excellent, keep it there. Two requests from that branch:
1. OBJECT COUNT: ~1,300 LOD wrappers plus their empty far-level groups are walked by three.js every frame; a 1,200 m level-0 cell (and 2,400 m level-1 cell) would halve the count for free. Do it if the vertex work stays bounded: measure with the same reasoning as the header (a cell only submits its instances while the camera is within range + the cell's reach).
2. START-UP: the vegetation scatter is one of the two items that make loadSite 0.7-1.0 s on the i7 (2-3 s on a phone). Time buildForest (a Node measurement with a stubbed field is fine; tools/dom-stub.mjs exists) and bring it down: fewer field.height calls (sample once per candidate, never twice), fewer field.forestNoise calls in the margin test (the 8-neighbour probe is 8 fbm evaluations per candidate - cache on a coarse grid), the hedge walk only over parcels near the flight region, and no per-tree THREE.Color allocation in the loop (compute HSL once per species and vary by a cheap per-tree factor). Target: under 150 ms on the i7 for the plains, under 250 ms for the mountain.

THE LOOKS THAT ARE STILL OPEN - your call on each:
3. THE FAR CARDS ARE LIT AS FLAT GROUND: with the normal forced to +Y every card takes the sun like a lawn, so a hillside of cards beyond 1.5 km reads flatter than the level-1 trees in front of it, and at low sun the far forest is too bright. Give the card its own lighting: a normal that leans toward the camera (mix of +Y and the view direction, say 0.6/0.4) or a baked lit/shaded silhouette pair chosen by the sun's side (you have the sun direction through the atmosphere uniforms atSun once installFog runs - or pass your own vgSun uniform set from the instance's chunk; note that the vegetation material does not get set() calls, so a baked pair in the texture selected by a per-instance dot product in the vertex shader is the robust route).
4. THE HEDGE LINE VANISHES BEYOND 2 KM: the hedges drop out at 2 km and the fields lose their edges from the air (the ground's parcel borders carry a thin line only). Either raise the hedge cut-off to 4 km with a cheaper far segment (a single quad strip per hedge run, 2 triangles per 12 m, instanced), or let the level-1 cards cover them; decide by cost.
5. Anything else you see in your own contract that the implementation missed: the coastal lean, the treeline larches, the saplings at the margins, the woodland clearings, the species mix per site.

RULES: keep the file header and update it; keep the exported names and shapes (buildForest(field, opts) -> { objects, count }, OBSTACLE_TREE in step with the obstacle spruce's real size, buildObstacleTrees); uniforms prefixed `vg`, never `at`; materials and geometries never cached module-wide (textures may be); name every material; a versioned customProgramCacheKey (bump 'ctl-vegetation-v1' when the injected code changes); no Math.random; frustumCulled left on with computed bounds; castShadow only on level 0.

CHECK YOUR WORK: `node tools/test-art.mjs` must pass. In your answer: what you changed, the buildForest timing before and after, the object count per site, and what these scenes should now look like: plains 10:00 chase (hedges to the horizon), the tower 16:30 (the far forest line), mountain 09:30 (the carpet on the walls, now with sides), ridge 17:00, coast 13:00.
