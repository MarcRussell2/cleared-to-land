CLEARED TO LAND art pass 1b of the world rebuild: THE SKY, second round - the review of pass 1 with the screenshots and measurements, and what to fix.

You are the art department. Read AGENTS.md at the root, then src/art/AGENTS.md (section "The world bench"), then src/art/README.md. Same fence as pass 1, THREE files: src/art/world-atmosphere.js, src/art/sky.js, and the `atmosphere` block of src/art/palette.js. Nothing else. Your pass-1 work is committed; edit it in place.

WHAT PASS 1 GOT RIGHT (keep it): the lookup-table atmosphere, the radiance budget on the lights (the runway markings no longer glow, the airliner is white not blown), Koschmieder aerial perspective, the night scene (a horizon you can find, a moonlit sea, stars, exposure 3.8), the moon, the stars faded by sky brightness.

WHAT THE SCREENSHOTS AND THE NUMBERS SHOW IS WRONG:

1. THE SKY ITSELF BREAKS THE BUDGET. tools/sky-budget.mjs (new, plain Node: `node tools/sky-budget.mjs`) bakes your LUT for the sheet's conditions and prints the peak; today:
     plains 10:00  sun 46 deg  peak 1.93 at x0 y13   horizon 0.28,0.35,0.39  zenith 0.02,0.04,0.08
     tower 16:30   sun 41 deg  peak 2.23 at x0 y13   horizon 0.27,0.33,0.37  zenith 0.02,0.04,0.08
     dusk 19:00    sun 7.5 deg peak 1.66 at x0 y13
     fog 07:00     peak 0.46 (zenith)                horizon 0.33
   x0 is the sun's direction, y13 the horizon: the sun-side aureole reaches 1.9-2.2 linear over a wide patch of sky, twice the bloom threshold, and the bloom (strength 0.35, radius 0.5, fixed in the engine) smears that into a grey veil over the whole frame. The tower scene (16:30, 25 km, camera 1.5 km from the airplane) is now a complete whiteout, worse than before your pass; coast and carrier-day are milky. At the same time the zenith is 0.02-0.08: the sky's range is inverted - a nearly black zenith and a blinding horizon.
   Required, checked by the tool (it must exit 0): every DAYTIME sky value at most 0.85 linear everywhere except the sun disc (celestial(), which is added by the dome only and may stay bright); the zenith at 10:00-13:00 around 0.12-0.22 (a deep saturated blue after ACES at exposure ~1.1), the horizon away from the sun 0.40-0.60, the sun-side aureole up to 0.85 and only within ~20-25 degrees of the sun; the horizon haze band brighter than the sky just above it but under 0.85; below the horizon (the LUT's negative-elevation rows, seen by the fog on ground pixels) 10-20% darker than the horizon row. Dusk 19:00 (sun 7.5 deg): peak under 0.85 too, warm orange near the sun, the sky above going through a pale band to blue-grey. The night rows are fine.
   Then retune the exposure so the daytime picture is bright: plains at 10:00 should read as a bright, contrasty morning - sunlit grass mid-tone, a deep blue zenith, crisp shadows - not pastel and not grey. Expect toneMappingExposure around 1.1-1.4 by day.

2. FOG (07:00, 700 m): a bright white slab. At 7.5 degrees of sun, 700 m visibility, the fog should be a dim blue-grey (0.22-0.32 linear at eye level), a little brighter overhead, darker below eye level, faintly warm toward the sun; the approach lights then read as lights in fog rather than a blob on white. The `mist` term: lower it and give it a vertical gradient; keep it tied to the sun's elevation.

3. CLOUDS: a visible grid. The puff texture repeats every 6.5 km on the low layer and every 12.5 km on the high one, and the puffs are near-identical round blobs at regular spacing, so the cockpit and coast scenes show rows and columns of identical clouds. Fix in cloudTexture(): place puffs in seeded clusters (6-10 clusters per tile, 4-14 puffs each, radii from 4 to 40 px, elongated along one wind direction, the cluster centres jittered well away from any grid), give each cluster a flatter base than top (the density gradient you bake carries the lighting), and break the period in the shader by sampling the same texture a second time at a different scale and rotation (for example 0.37x, rotated 40 degrees) and combining the two densities (a product or a soft max), so no repeat is visible across the 60 km sheet. Keep two layers; raise the low tile to 9-10 km. Lighting: undersides darker, a bright rim toward the sun, bluer with distance; keep coverage from opts.cloudCover.

4. Small things: the horizon haze band should be visible as a band in every clear scene (it is, keep it); the carrier-day sky should not be a flat pale grey - at 14:00, 30 km, it is blue overhead and pale toward the horizon; the sea's grazing reflection (world-water.js, not yours) reflects your horizon rows, so a horizon under 0.85 also fixes the too-bright far sea.

DO NOT re-open what works: keep the SkySystem API, the uniform names (all existing ones stay meaningful: world-water.js reads atSun, atDay, atLow, atWeather, atWarm, atMoon; installFog reads atDay, atWeather, atExtinction), the exported names, the file headers (update them), material names, no Math.random, no module-level material caches, everything prefixed `at` in these files only.

CHECK YOUR WORK: `node tools/test-art.mjs` must pass and `node tools/sky-budget.mjs` must exit 0; paste the sky-budget table in your answer. Then describe what these scenes should now look like: plains 10:00 40 km chase; the tower 16:30 25 km (fields legible, the airplane clearly visible against the sky, warm afternoon light); coast 13:00; carrier 14:00; fog 07:00 700 m; cockpit 10:00 (the clouds); the menu backdrop 08:30 (a low warm sun; note the earlier dark menu shot was a harness fault, since fixed, not yours).
