CLEARED TO LAND art pass 2b of the world rebuild: THE GROUND, polish round - the shader's cost, the build time, and the far tiles.

You are the art department. Read AGENTS.md at the root, then src/art/AGENTS.md (section "The world bench"), then src/art/README.md, then docs/PERF.md sections 5 ("terrain-look.js"), 9.1 and 9.3. Same fence as pass 2, THREE files: src/art/world-ground.js, src/art/textures.js (groundDetail and the ground's own textures only), src/art/palette.js (the `ground` and `groundDetail` blocks only). Nothing else. Your pass 2 is committed and accepted for its look; edit in place and keep that look: every scene on the owner's sheet must read the same after this pass (a lookdiff under ~8/255 per scene is the target; the parcels, headlands, tracks, the site colouring, the coast's sand, the mountains' rock and snow all stay).

MEASURED REQUIREMENTS from the performance branch (docs/PERF.md 9.3, the Pixel proxy = Intel UHD 770 at the phone's medium tier, plains scene):
1. THE GROUND SHADER IS 3.2 ms OF A 5.7 ms FRAME - more than half of it - and must come down to about 2 ms. Where the time goes: 24 LOD tiles receiving PCF shadows, the parcel block (a nearest-filtered lookup, fract/floor/fwidth per fragment, the crop rows), the two detail-texture taps plus the relief term. Asked:
   - Skip the whole parcel block where `farm == 0` (the vertex attribute is 0 on the mountains, the coast's shore, near the runway flats and everywhere the slope is steep): `if (farm > 0.001) { ... }` around it, so a mountain site pays nothing for parcels. Branching on a varying that is uniform across a triangle is cheap on every GPU here.
   - Fewer taps: one detail tap in the far levels (the 80/240 m tiles never show the near layer anyway - select by a per-level uniform or by the near-layer fade being 0 at those distances so the compiler can skip it; two taps only where apNear > 0), and drop the relief term beyond ~600 m.
   - Cheaper far LOD: the 240 m tiles (and the outer tiles) can use a simpler material variant (no parcel block, one tap, no relief; vertex colours and fog only) - a second MeshStandardMaterial with its own customProgramCacheKey is fine; they are still "world/ground", name them 'world/ground-far'.
   - The skirts: keep them thin (2-3 m) so their overdraw is small; make sure they are not double-drawn where two levels meet.
   - Keep receiveShadow on all levels (the airplane's shadow in the flare is the game).
2. START-UP: loadSite went from ~300 ms to 676 ms on the plains, 787-840 on the coast and 983 on the mountain (i7), which is two to three seconds of black after "Fly" on a phone; the near ground grids at 20 m across the 4 km flight region are the biggest single item (the vegetation is the other and is another brief). Target: buildGround under 200 ms on the i7 for the plains. Options, your choice: build the near (20 m) level lazily on the first frame it is needed (a THREE.LOD level may start as an empty Group that is replaced by the real mesh on the LOD's first update - use the mesh's onBeforeRender or a cheap check in a per-chunk update, NOT an engine call); sample field.height on a coarser grid and interpolate the in-between vertices (the heightfield is smooth); build the far tiles at 240 m only outside the 4 km region; reuse one PlaneGeometry index buffer for all chunks of a level (only positions/colours differ). Print your timing in the answer (a quick Node measurement with a stubbed field is fine - tools/dom-stub.mjs exists for canvas-free imports).
3. TEXTURES: the site texture budget is 12 MB and the ground's share is small (a 1024 px detail); keep it.

RULES: keep the file headers and update them; keep the exports and signatures (groundColor, buildGround(field, opts), parcel); no `at*` uniforms (prefix `gr`); no module-level material caches (textures may be); name every material; versioned customProgramCacheKey (bump 'ctl-ground-v2' when the injected code changes); no Math.random; nothing here changes a height (field.height is the only source of heights).

CHECK YOUR WORK: `node tools/test-art.mjs` must pass. In your answer: what you changed, the expected shader cost reduction, the buildGround timing before and after, and a note that the look is unchanged (the owner diffs every scene against the previous sheet).
